extends ../layout

block content
  #subscriptions
    form#addSub(action="/api/users/#{user.getHash()}" method="put" data-id="#{user.getHash()}")
      input(type="hidden" name="_csrf" value=_csrf)
      .form-group
        input#searchSub(type="text")
        button(type="submit") Add
    ul#subsList
